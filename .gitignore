# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db

# Local machine-specific stuff
*.local
calibration/          # if any local calibration cache lands in the repo dir
*.log

# MediaPipe model (auto-downloaded on first run)
*.task

# Machine-specific calibration (captured per-rig, never shared)
arm_geom.json
rest_pose.json
homography.npz

# Never commit photos/screenshots of the rig (they contain the room, faces, etc.)
*.jpg
*.jpeg
*.png
*.gif
*.mp4
*.mov

# YOLO weights (auto-downloaded)
*.pt
pick_tune.json

# Dance: downloaded audio, cached dance sheets, captured stage pose
dance/songs/
*.dance.json
dance/stage_pose.json
